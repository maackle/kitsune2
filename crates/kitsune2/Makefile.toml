extend = "../../Makefile.toml"

[tasks.clippy]
args = [
  "clippy",
  "--all-targets",
  "--features",
  "datachannel-vendored",
  "--",
  "--deny=warnings",
]

[tasks.test]
args = ["test", "--no-default-features", "--features", "datachannel-vendored"]
